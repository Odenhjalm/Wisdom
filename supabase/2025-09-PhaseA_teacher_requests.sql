-- Phase A: teacher_requests migration intentionally skipped.
-- Teachers will be assigned manually (e.g. via admin flag / role management).
-- This placeholder keeps the migration list consistent without performing any data moves.

begin;
-- no-op
commit;
